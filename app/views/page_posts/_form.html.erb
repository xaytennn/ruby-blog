<%= form_with(model: page_post, local: true) do |form| %>
  <% if page_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(page_post.errors.count, "error") %> prohibited this page_post from being saved:</h2>

      <ul>
        <% page_post.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
